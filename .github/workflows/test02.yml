name: compile_and_test_02

on:
  push:
    branches:
      - main

defaults:
  run:
    shell: bash

jobs:
  build_and_test:
    name: Build and test assignment 2
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3
        
      - name: Compile and Test
        run: sh ./scripts/test02.sh
        
    
